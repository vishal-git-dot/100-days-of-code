
# Day 28 – Functions and Lambda Expressions in Python

## 📌 Topics Covered
- User-defined functions
- Return values
- Loops inside functions
- Lambda (anonymous) functions
- filter()
- reversed()

---

## ✅ Functions Implemented

### 1️⃣ Sum Function
Returns formatted sum of two numbers.

### 2️⃣ Print List Function
Loops through a list and prints each element.

### 3️⃣ Area of Circle
Calculates area using formula:
area = 3.14 * r²

### 4️⃣ Capitalize Function
Converts string to uppercase using `.upper()`

---

## 🔥 Lambda Functions

### ✔ Multiplier (Closure Example)
Created a function that returns another function using lambda.

### ✔ Adder Function
Returns lambda that adds a fixed number.

### ✔ Filter Even Numbers
Used `filter()` with lambda to extract even numbers from a list.

### ✔ Square Function
Lambda returning square of a number.

---

## 🔁 Built-in Function Practice

### ✔ reverse()
Used `reversed()` to reverse a string.

---

## 🎯 Learning Outcomes
- Understood function creation and return values
- Practiced lambda expressions
- Learned closures (function returning function)
- Used higher-order functions like filter()
- Strengthened problem-solving with small utility functions

---

## 🚀 Suggested Improvements
- Use `math.pi` instead of 3.14
- Add input validation
- Try using `map()` with lambda
- Convert some functions into one-liners
